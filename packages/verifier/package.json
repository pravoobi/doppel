{
  "name": "@doppel/verifier",
  "version": "0.1.0",
  "private": true,
  "description": "Sandbox orchestration, harness generation, and image diffing (CLAUDE.md §5.7). sandbox.ts is a stub pending Sandboxes beta access; everything else here is sandbox-independent and fully tested.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@doppel/shared": "workspace:*",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pixelmatch": "^5.2.6",
    "@types/pngjs": "^6.0.5",
    "ts-morph": "^23.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
