{
  "name": "@doppel/db",
  "version": "0.1.0",
  "private": true,
  "description": "Drizzle schema and client for the runs/findings/llm_calls datastore (CLAUDE.md §6).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "node scripts/apply-migration.mjs",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@doppel/shared": "workspace:*",
    "@neondatabase/serverless": "^0.10.1",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
